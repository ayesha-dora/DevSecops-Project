# Upload a filled copy as a Jenkins Secret file, ID: devsecops-local-env.
# Docker env-file format: KEY=value, without quotes or export.
# Generate a value using: openssl rand -hex 32
APP_API_KEY=CHANGE_ME
LOG_LEVEL=INFO
